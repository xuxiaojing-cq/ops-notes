1. 确认进程状态 → 0 个进程，但 CLI 可用
   → 结论：客户端正常，服务端未运行

2. 检查 App 完整性、磁盘空间 → 发现只剩 27G + 更新残留半成品
   → 第一轮根因：自动更新中断
   → 处置：精准清理残留（不重装，保住 7.1G 镜像数据）

3. 清理后仍失败 → 重新采集，问题已变化
   → 教训：修复一个问题后必须重新验证，不能假设是同一个原因

4. 读日志定位：VM 卡在 starting 被 terminated signal 杀掉
   → ⚠️ 干扰项识别：日志里最显眼的 "TLS handshake failure" 只是后台检查更新失败，与本问题无关

5. console.log 为空 → 决定性证据：VM 内核从未 boot
   → 说明不是 Docker 内部错误，是外部阻断

6. 排除 App 自身问题：codesign 校验通过、虚拟化文件完整
   → 范围收敛到系统层

7. 检查系统环境 → 发现 ESET EDR + DLP 企业管控组件
   → 最终根因：安全软件拦截 Virtualization.framework

8. 决策：不绕过（合规风险 + 技术不可行），改用在线沙箱替代
